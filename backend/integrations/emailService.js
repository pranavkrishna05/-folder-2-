const nodemailer = require('nodemailer');const transporter = nodemailer.createTransport({  service: 'gmail',  auth: {    user: process.env.EMAIL_USER,    pass: process.env.EMAIL_PASS  }});const sendConfirmationEmail = async (email) => {  const mailOptions = {    from: process.env.EMAIL_USER,    to: email,    subject: 'Account Confirmation',    text: 'Thank you for registering! Please confirm your email address.'  };  await transporter.sendMail(mailOptions);};module.exports = { sendConfirmationEmail };